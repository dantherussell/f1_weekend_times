<%= link_to "Home", root_path %>
<br><br>

<%= @weekend.gp_title %>
<br><br>
<%= @weekend.location %>
<br><br>
<%= @weekend.timespan %>
<br><br><br>

Events:<br><br><br>
<% @events.each do |day, events| %>
  <%= day %>
  <br><br>
  <% events.each do |event| %>
    <%= "#{event.racing_class} #{event.name} -- #{event.circuit_time}" %> (<%= local_time(event.start_time, "%H:%M") %> local) | <%= link_to "Edit", edit_weekend_event_path(@weekend, event) %> | <%= link_to "Delete", weekend_event_path(@weekend, event), method: :delete, data: { confirm: "Are you sure you want to delete this event?" } %>
    <br><br>
  <% end %>
  <br>
<% end %>

<%= link_to "New Event", new_weekend_event_path(@weekend) %>
<br><br>
<%= link_to "Edit Weekend", edit_weekend_path(@weekend) %>
<br><br>
<%= link_to "Delete Weekend", weekend_path(@weekend), method: :delete, data: { confirm: "Are you sure you want to delete this weekend?" } %>
<br><br>

Admin functions use a cookie but I store zero information besides that you've logged in here. Check the source code if you want to be sure.
